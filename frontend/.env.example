# ============================================================================
# Frontend Environment Variables - Example Configuration
# Copy this file to .env.local and update values for local development
# ============================================================================

# API Configuration
# -----------------
# Base URL for backend API calls
# Development: http://localhost:8080/api/v1
# Production: Set via Docker build args or deployment environment
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# Application Configuration
# -------------------------
# Node environment (development, production, test)
NODE_ENV=development

# Next.js Configuration
# ---------------------
# Port for development server (default: 3000)
PORT=3000

# Hostname for server binding (default: 0.0.0.0 for Docker)
HOSTNAME=0.0.0.0

# ============================================================================
# NOTES:
# ============================================================================
#
# 1. NEXT_PUBLIC_* variables are exposed to the browser
#    - Be careful not to include sensitive data
#    - These are baked into the JS bundle at build time
#
# 2. For Docker builds:
#    - NEXT_PUBLIC_API_BASE_URL is set via build args in docker-compose.yml
#    - Do not commit .env.local to version control
#
# 3. For local development:
#    - Copy this file to .env.local
#    - Update NEXT_PUBLIC_API_BASE_URL if backend runs on different port
#
# 4. For production deployment (Cloud Run):
#    - Set environment variables in deployment configuration
#    - NEXT_PUBLIC_API_BASE_URL should point to production API
#
# ============================================================================
